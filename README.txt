This code is for visual inspection only and cannot be executed since the necessary data is not included.

The code for training machine learning models is developed in Jupyter Notebook. Therefore, having Jupyter Notebook installed is a requirements to properly view the files.